paths:
  experiment_prefix: raw_zeroshot

train:
  context_mode: raw_text
  finetune: false
  max_seq_length: 8192   # placeholder -- pick from measured context_length_stats.json before running depth-5 for real
  on_context_overflow: warn

eval:
  context_mode: raw_text
